# .github Repository

This repository defines the **organization-wide defaults** for the Carbon Development Platform.

It provides shared community health files that are automatically applied to all repositories in the organization.
